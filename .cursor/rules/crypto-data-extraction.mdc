---
description: Правила для извлечения и обработки данных крипто-проектов
---

# Извлечение данных крипто-проектов

## Целевые данные

Из каждого крипто-проекта извлекаем:

- **currentPrice** - текущая цена токена
- **nextPrice** - следующая цена (если есть этапы продаж)
- **listingPrice** - цена при листинге на биржах
- **raised** - собранные средства (raised funds)

## Форматы данных

Поддерживаемые форматы цен:
- `$0.000443` - доллары с точкой
- `0.000443 USDT` - с валютой
- `$0.000443 USD` - полное название валюты
- `443` - только число (предполагаем микро-доллары)

Поддерживаемые форматы собранных средств:
- `$8.9M` - миллионы
- `$2,000,000` - полная сумма
- `8.9M USD` - с валютой
- `89%` - процент от цели

## Селекторы для скриншотов

Типичные CSS селекторы для крипто-проектов:

```css
/* Ценовые виджеты */
.price-widget, .token-price, .current-price
.price-display, .price-info, .token-value

/* Информация о токене */
.token-info, .token-details, .coin-info
.token-stats, .token-metrics

/* Прогресс сбора средств */
.progress-bar, .funding-progress, .raise-progress
.progress-info, .funding-stats

/* Общие контейнеры */
.main-content, .hero-section, .token-section
.dashboard, .overview, .stats-container
```

## Множественные селекторы

Для сложных страниц используйте несколько селекторов:

```json
{
  "url": "https://example-ico.com",
  "selectors": [
    ".price-section",      // Основная цена
    ".token-info",         // Дополнительная информация
    ".progress-bar",       // Прогресс сбора
    ".stats-container"     // Общая статистика
  ]
}
```

## Валидация данных

Проверяйте извлечённые данные:

- Цены должны быть положительными числами
- Собранные средства не должны превышать разумные лимиты
- URL должен быть валидным
- Временные метки должны быть корректными

## Обработка ошибок

- Если данные не найдены - возвращайте "N/A"
- При ошибке парсинга - логируйте и продолжайте
- Объединяйте результаты из разных селекторов
- Приоритет: реальные данные > "N/A" > пустые значения

## Примеры результатов

```json
{
  "currentPrice": "$0.000443",
  "nextPrice": "N/A", 
  "listingPrice": "$0.005242",
  "raised": "$8.9M",
  "url": "https://example-ico.com",
  "timestamp": "2024-12-15T14:30:25.000Z"
}
```